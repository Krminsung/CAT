# [cat][P12] tasks·worktree·SSH/clipboard

## 범위

session-owned background process, 현재 저장소 안의 managed Git worktree, 사용자 요청에서만 시작되는
OSC52/tmux/SSH clipboard 보조 흐름을 구현한다. 원본 `src/tasks.ts`, `src/worktree.ts`,
`src/clipboard.ts`, `src/ssh-clipboard.ts`, `src/cli.ts`와 관련 원본 테스트 계약만 읽으며 실제 셸·task,
Git worktree 생성·삭제, SSH/PTTY, clipboard와 앱은 실행하지 않는다.

P11은 검토 head `638fcfee9379ca4a5008829351e7449ba53ee034`를 PR #11에서 merge commit
`05a02e75e795e8079b318b2168fc372c50025c7b`로 병합했다. 두 부모와 동일 tree, phase head 조상 관계,
`origin/main` 포함을 확인했고 병합 뒤 검사는 실행하지 않았다.

## 하위 작업 커밋

| 작업 | commit | 변경 요약 |
|---|---|---|
| P12.1 | `b7d78eb02eec6f77509c68127fd35d2c638729b7` | session 소유권·deadline·8MiB tail·stale 복구를 갖춘 background task manager |
| P12.2 | `e72579c98e997127a6565ea57b86c2c088c9a193` | 현재 repo 안의 guarded managed worktree create/list/remove |
| P12.3 | `d6c9e36cfa740891c51adf361b1371376308b71f` | 사용자 주도 OSC52/tmux 및 bounded SSH PTY bridge |
| P12.4 | 작업 중 | `/tasks`, `/worktree`, `! command &`, 표시와 종료 cleanup 연결 |

## P12.1 구현 메모

- `run_command`는 foreground의 `timeout_seconds`와 background의 별도 `deadline_seconds`를 함께 받지
  않는다. 명령 문자열의 단독 `&`는 manager 소유 process group 밖으로 빠질 수 있어 거부하고,
  background 시작도 기존 중앙 command 승인·workspace identity 재검사를 통과한다.
- manager는 canonical workspace hash별 `CAT_HOME/tasks` 하위 0700 저장소에서 16자리 무작위 task ID,
  0600 manifest·log를 사용한다. 한 process의 동시 작업 8개, session 기록 64개, 전체 기록 128개,
  기본 1시간·최대 24시간 deadline과 제한된 복원 scan을 적용한다.
- stdout과 stderr는 32-byte 상태 header가 있는 disk-backed 8MiB 원형 tail에 합치며 read 크기와 tool
  output도 별도로 제한한다. terminal C0/C1 control을 실행 불가능한 문자로 바꾸고 출력 저장·읽기 오류와
  생략 상태를 숨기지 않는다.
- list/read/stop은 현재 session의 task ID 또는 모호하지 않은 prefix만 해석한다. stop 승인은 task ID,
  session, 시작 시각과 command digest를 묶고, 같은 manager가 실제 child reference로 소유한 process
  group에만 TERM→KILL을 보낸다. 임의 PID 입력과 복원된 PID 신호 경로는 없다.
- manifest에는 종료 확인 여부를 기록한다. 재시작 시 확인되지 않은 starting/running/종료 중 기록은
  `stale`로 복원하고 PID를 추측하지 않는다. lifecycle 격리 정리는 현재 process가 만든 task만 종료해
  재개한 session에서 stale 상태를 조회할 수 있으며, manager close 실패는 명시적으로 보고한다.

## P12.2 구현 메모

- `CAT_HOME/worktrees` 아래에는 Git common directory의 canonical path·device·inode를 해시한 저장소별
  0700 영역과 0600 registry를 둔다. registry에는 cat이 만든 이름·고정 경로·branch·base·생성 시각과
  filesystem identity만 기록하며, 다른 repository identity의 기록이나 중복·범위 밖 경로는 거부한다.
- create/list/remove는 shell 문자열이 아닌 제한된 Git argv와 NUL 구분 porcelain을 사용한다. Git
  prompt·pager·hook과 injection environment를 끄고 30초·1MiB 상한을 적용한다. create는 registry에
  `creating`을 먼저 예약하고 실제 path·Git 등록·branch identity를 확인한 뒤에만 `active`로 확정한다.
- remove는 registry에 등록되고 생성 시 directory identity까지 확정된 항목만 대상으로 삼는다. 현재
  process cwd나 선택한 `-C` cwd를 포함한 worktree, locked·bare·prunable 상태, identity 변경과 tracked,
  untracked, ignored 파일이 하나라도 있는 상태는 거부한다. `--force`, prune 및 branch 삭제 경로는 없다.
  결과가 불확실하면 registry를 `unknown`으로 남기며 외부 path나 PID를 추측해 정리하지 않는다.
- `cat-tui worktree add|list|remove`와 `-w/--worktree [name]`, `/worktree` 정보 overlay를 연결했다.
  `-w`는 기존 workspace의 trust가 결정된 뒤에만 생성하고 새 filesystem cwd에서 storage path,
  project customization과 trust를 다시 해석한다. 설정의 선택적 `worktree.baseRef`도 신뢰된 계층만
  사용하며 다른 managed worktree 저장소는 파일·command 도구의 민감 경로로 차단한다.

## P12.3 구현 메모

- 기본 clipboard writer는 사용자가 `/raw copy`를 입력하거나 selection action을 요청한 경우에만
  로컬 desktop adapter를 시도하고, 사용할 수 없을 때 host가 직접 만든 OSC52 write로 fallback한다.
  OSC52는 100,000-byte base64와 약 75KiB UTF-8 상한을 두며 tmux의 escaped DCS passthrough 및 GNU
  screen의 제한된 chunk wrapping을 적용한다. clipboard read/query API는 추가하지 않았다.
- `cat-tui ssh`는 접속하는 PC의 세 stdio가 모두 TTY인 명시적 관리 명령으로만 실행된다. OpenSSH
  option은 shell command로 합치지 않고 검증한 argv와 `shell: false`로 넘기며 원격 PTY를 강제한다.
  child environment는 최소 allowlist이고 검증한 `SSH_AUTH_SOCK`만 이 전용 경로에 전달하며 option,
  key path, agent socket이나 credential을 진단에 출력하지 않는다.
- SSH stdout과 stderr는 별도 streaming UTF-8 state machine을 통과한다. OSC/DCS/SOS/PM/APC 문자열과
  C1 control은 터미널로 전달하지 않고, OSC52의 제한된 selection·canonical base64·fatal UTF-8 write만
  최대 32건 수락한다. `?` read는 응답 없이 차단하며 payload·parser buffer·input chunk와 대기 queue를
  제한한다. 복사 요청은 각각 1.5초, 종료 drain은 7초 상한을 갖는다.
- bridge는 output backpressure를 따라 child stream을 멈췄다가 재개한다. SIGWINCH와 종료 signal은
  같은 호출에서 만든 process group에만 전달하고 TERM 계열 뒤 KILL·강제 close를 제한 시간 안에
  시도한다. 종료를 확정하지 못하면 성공 코드로 숨기지 않고 고정된 terminal restore sequence와
  확인 안내를 남긴다.

## P12.4 구현 메모

- `/tasks` 목록·출력·중지는 각각 `list_tasks`, `get_task_output`, `stop_task` 중앙 실행 경계를 그대로
  사용한다. ID는 현재 session의 1–16자리 hex prefix만 받고 중지는 기존 command 승인을 거친다.
  화면 출력은 12KiB로 제한하고 목록의 긴 command와 오류는 접두부를 남기지 않는 고정 생략 문구로
  대체해 전체 JSON이 구조를 잃거나 redaction 경계가 잘리지 않게 한다.
- 사용자가 직접 입력한 `! command &`는 foreground `timeout_seconds`를 보내지 않고 1시간
  `deadline_seconds`가 있는 managed background 요청으로 변환한다. 시작 결과의 정확한 task ID만 다음
  model 요청의 비신뢰 local context와 상태 안내에 남기며 실제 단독 `&`는 shell command에 전달하지
  않는다.
- manager의 bounded change listener와 session별 overview로 active·unconfirmed task 수를 TUI header와
  `/status`에 표시한다. 자동·수동 compaction에는 최대 8개 non-terminal task snapshot만 비신뢰
  incomplete work로 남겨 장기 작업을 완료로 오인하지 않게 하며 command는 전체 secret redaction 뒤
  표시 길이를 줄인다.
- session lifecycle cleaner와 앱 종료 manager close는 현재 process가 소유한 task만 정리한다. 공개
  transport, MCP, hook 또는 session 기록 중 일부가 실패해도 background close를 건너뛰지 않으며 종료
  미확정은 전체 성공으로 숨기지 않는다.

## 정적 코드 검토

P12.1–P12.3을 커밋했고 P12.4의 중앙 task 권한, session별 표시·continuity, foreground/background 시간
계약과 오류가 있어도 이어지는 종료 cleanup을 소스 기준으로 대조하고 있다. 실제 process, Git
worktree, SSH/PTTY, clipboard, tool handler, 앱과 검증 명령은 실행하지 않았다.

## 검증 상태

- 허용 명령: `npm run check`
- attemptsUsed / maximumAttempts: 0 / 1
- 결과: NOT_RUN
- 검사 대상 commit SHA: 미예약

## 병합 정합성

- base main SHA: `05a02e75e795e8079b318b2168fc372c50025c7b`
- 예정 브랜치: `cat/p12-worktree-tasks`
- PR/merge: NOT_PUBLISHED

## 다음 작업

P12.4 정적 검토와 하위 작업 커밋을 마친 뒤 P12 전체 diff와 base 정합성을 확인하고 검증을 예약한다.
