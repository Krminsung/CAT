# P14.R09.UI — 터미널 가시성 개선

현재 결과(2026-09-21): **승인된 범위의 빌드·로컬 실행 검증 통과, 원격 게시 전**.
마지막 5차 실행은 초기 설정과 남은 PTY 시나리오 7개를 통과했다. 이번 승인에 포함된
추가 재시도 2회는 사용하지 않았다. 아래 승인 대기·실패 기록은 이전 시점의 이력이다.

## 범위와 진행

2026-09-21 사용자 요청: 고양이 ASCII 시작 화면, 작업 중 로딩, 대화 역할 구분, 읽기 쉬운 세션
표시와 권한 팝업 겹침 개선. 앞선 시간 제한 제거 커밋 `8b6c75b8731901e3ffbe71611423ca7edeaf79d1`을
보존하고 같은 P14 후속 작업에 포함한다. 새 검증 단계로 이전 검사 예산을 초기화하지 않는다.

## 변경

- `presentation.ts`: 고양이·시작 안내, 대화 제목 머리글, 역할별 테두리·색상과 실행 상태 표시.
- `input.ts` / `screen.ts`: 실제 입력 처리의 busy 변경을 연결한다. 시작 인자 prompt와 일반 입력,
  세션 단축키 처리에도 로딩을 표시하고 종료·취소·오류·화면 닫기에서 timer를 정리한다.
- 모델 응답 대기/작성, 도구 실행, 다음 단계 판단, 사용자 확인, 압축과 결과 정리를 표시한다.
  텍스트 응답이 끝난 것은 전체 작업 완료로 취급하지 않는다. 완료와 오류·취소 표시를 구분한다.
- `transcript.ts`: 나/CAT 역할 이름과 서로 다른 세로선·색상, 메시지 사이 여백을 사용한다.
  표시 cache를 재사용하고 항목당 최대 512행 이후에는 생략 안내를 둔다. 원문 복사에는 장식을 넣지 않는다.
- `dialog.ts`: 불투명 전체 배경, 중앙 패널, 본문 페이지 스크롤과 고정 선택지·키 안내.
  작은 창에서는 보이지 않는 선택을 확인하지 않는다. 입력 패널의 커서·masked key 흐름은 유지한다.
- `application.ts`: 긴 세션 ID 대신 새 대화/첫 요청 제목을 표시하고 `/rename`·세션 전환과 연결한다.
  세션 선택 목록은 이름·날짜·짧은 보조 ID를 쓰며 실제 저장 ID와 재개 계약은 변경하지 않는다.
- 색상은 화면 boundary의 redaction/제어문자 제거 다음에만 생성한다. `--no-color`, `NO_COLOR`,
  `TERM=dumb`에서는 색상을 추가하지 않으며 역할·테두리·불투명 배경은 유지한다.
- 권한 정책·승인 빈도·자동 허용 범위, 실제 사용자 데이터와 Release/installer는 변경하지 않는다.

## 소스 검토

입력 busy의 true/finally false, run_end와 텍스트 종료의 구분, timer cleanup, modal focus·취소·
크기 변경, 입력 커서 표식, Unicode 표시 폭, redaction 뒤 색상 생성, session rename 호출을 읽어 검토했다.
신규 파일과 변경 diff를 확인했으며 정적 컴파일이나 런타임 결과로 표현하지 않는다.

## 검증 승인 대기

UI와 앞선 시간 제한 변경을 합친 `npm run build` 1회와 로컬 검증 1회를 요청했다.
아직 승인되지 않았으므로 build/check·앱 실행·테스트는 수행하지 않았다.
검증 시에는 임시 CAT_HOME과 로컬 모의 API만 사용하고 실제 계정·유료 외부 API는 사용하지 않는다.

확인할 범위:

1. 새 대화 ASCII, 한글·줄바꿈 메시지의 역할 구분과 첫 요청 제목, `/rename`·재개.
2. 응답 지연과 텍스트 종료 후 도구/다음 요청 동안 spinner·경과 시간이 유지되는지.
3. 완료·오류·Ctrl+C 취소 후 입력 복귀와 timer 정리.
4. 긴 권한 설명의 페이지 이동, 선택지·취소, 작은/좁은 창과 resize, 색상 끄기.
5. masked key 입력과 modal 종료 후 포커스·커서 복귀, raw 복사·JSON에 색상 미혼입.
6. 공통 실행 예산에서 시간 경과만으로 종료하지 않으며 사용자 취소·횟수 제한은 유지되는지.

실제 PTY/서버 UI는 현재 미검증이다. 소스 커밋만 준비하며 push·PR·main 병합·새 Release는 하지 않았다.
기존 공개 v0.1.3 installer에는 시간 제한 제거와 이번 UI 변경 모두 아직 반영되지 않았다.

## 2026-09-21 실행 승인과 예약

사용자가 “실행하고 검증하세요”로 위 빌드 1회·로컬 검증 1회를 승인했다.
소스 커밋 `0b7f831`에 대한 build 시도를 1회 예약한다. 빌드 성공 후 같은 소스로 실제 PTY와
로컬 모의 API, 예산·취소 검증을 진행한다. 실패하면 결과를 보존하고 수정·반복 실행하지 않는다.
공개 게시·installer 포장·원격 push·PR·병합은 이번 실행·검증 요청에 포함하지 않는다.

- `timeout 120s npm run build`: 1회 통과, 종료 코드 0, Node.js v24.21.0/npm 11.19.0.
- 빌드 대상: `7e8bae5332193b2c3306d1539929b4ec9b4d1185` (소스 `0b7f831`, 이후 차이는 예약 문서뿐).
- 로컬 검증 1회 예약: `.git/cat-progress/P14-ui-flow.py`가 예산·표시 구성 요소 검증과 실제 PTY의
  초기 설정·도구 실행·권한·취소·재개를 같은 빌드로 확인한다. 설정/로그는 임시 경로에만 쓴다.

## 2026-09-21 실행 결과 — 전체 검증 미완료

- 빌드 1회 PASS. 로컬 검증 1회는 `pty_text_then_tool_activity`에서 종료 코드 1로 중단됐다.
  두 실행 예산을 모두 사용했으며 재실행·앱 코드 수정·추가 빌드를 하지 않았다.
- 증거 경로: `/tmp/cat-ui-r09-w_f_v2qy`. `component-results.json`, `result.json`,
  `primary.terminal.log`, 구성 요소 화면 기록과 임시 세션 transcript를 보존했다.
- 실제 계정·외부 API 대신 임시 CAT_HOME, 가짜 키, loopback 모의 API를 사용했다.
  중단 후 검증용 자식 프로세스와 모의 서버를 정리했다.

통과 범위:

1. 구성 요소·예산 6개 사례: 전체 시간 타이머 미등록, 가상 시계의 10분 전후 및 최대 24시간
   경과에도 실행 유지, 사용자 취소, 각 횟수 제한과 cleanup. **실제 24시간 연속 실행 검증은 아니다.**
2. 구성 요소 수준의 전체 화면 권한 창, 페이지 이동, 24×10~200×60 표시와 20×8에서 Enter 차단,
   한글·emoji 폭, 역할 구분, 제어문자·비밀값 제거와 원문/JSON에 UI 색상 미혼입.
3. 구성 요소 수준의 spinner 변화, `text_complete` 후 busy 유지, 완료·실패·취소 구분.
4. 실제 PTY에서 custom provider 초기 설정, HTTP 명시적 선택, masked key 입력, 모델 선택,
   고양이 ASCII와 새 대화 머리글 표시. 자격 파일 권한과 키의 터미널 미노출도 확인했다.

중단 원인:

검증 스크립트가 모의 모델의 “로컬 서버로 실행하여 확인” 문구를 반드시 기다리도록 작성됐다.
그러나 기존 `src/agent/runner.ts`의 `suppressWebDraft` 경로는 웹 근거 정책이 응답 보류를
요구하는 경우 도구 호출에 동반된 중간 텍스트를 버린다. 실제 transcript에는 텍스트 이벤트 없이
도구 호출·`tool_start`가 기록됐고, PTY에는 실행 승인 창이 표시돼 있었다. 따라서 앱 종료가 아니라
**표시가 보장되지 않는 중간 텍스트를 요구한 검증 조건 오류**로 판단한다. 권한 선택은 전달되지
않아 도구 명령은 실행되지 않았다. 이 결과만으로 전체 앱 흐름에 결함이 없다고 판단하지 않는다.

미확인 범위:

- 실제 PTY에서 텍스트 이후 도구 작업·다음 모델 요청 동안의 로딩 유지와 최종 완료.
- 긴 권한 창의 실제 입력·resize, 오류·Ctrl+C 후 복귀, modal/raw 포커스 복귀.
- 실제 `/rename`·재개, `--no-color`·`NO_COLOR` 전체 흐름.

상태를 `BLOCKED_VERIFY`로 기록한다. 앱 소스·권한 정책은 그대로 두고, 중간 텍스트가 필요한
사례를 로컬 작업으로 명확히 구성하고 실제 권한·진행 상태에 맞게 검증 조건을 보완하는 승인과
남은 로컬 실행 검증 추가 1회를 요청한다. 소스 변경이 없으면 기존 빌드를 재사용하므로
추가 빌드는 필요하지 않다. push·PR·병합·installer·Release는 하지 않았다.

## 2026-09-21 추가 승인 — 로컬 실행 검증 2차 예약

사용자 “네 진행하세요”는 검증 스크립트 수정과 기존 빌드로 로컬 검증 추가 1회 요청에 대한 승인이다.
앱 코드·설정·빌드 입력은 변경하지 않는다. 빌드 시도는 1회 그대로이며 로컬 실행 시도만 2회로 예약한다.

- 이미 통과한 구성 요소·예산 6개 검사는 반복하지 않고 기존 결과를 사용한다.
- 새 임시 CAT_HOME·작업 폴더·가짜 키·loopback API로 남은 PTY 시나리오를 진행한다.
- 중간 문구 표시가 필요한 입력은 로컬 프로젝트 작업임을 명시하고 모의 SSE의 텍스트/도구 chunk
  사이에 표시 시간을 둔다. 실제 승인 창을 기다린 다음 앞선 텍스트 표시를 확인한다.
- 승인 후 실행 중인 명령의 표식·spinner·상태 문구를 함께 기록하고 동일 명령의 완료를 확인한다.
  소스상 `approval_required` 뒤 상태가 실행으로 돌아오지 않을 가능성이 있어 문구 전환도 확인한다.
- 누적 로그의 화면 순서와 실제 보이는 영역을 혼동하지 않도록 raw/재개 확인 조건도 보완했다.
- 실패하면 다시 수정·실행하지 않고 중단한다. 재빌드·앱 수정·원격 게시 승인으로 확대하지 않는다.

## 2026-09-21 2차 실행 결과 — 승인 후 상태 문구 결함 확인

`timeout 180s python3 .git/cat-progress/P14-ui-flow.py`를 추가 1회 실행했고,
`pty_text_then_tool_activity`의 상태 문구 검사에서 종료 코드 1로 중단했다.
실행 대상은 `822a52ff842f2e2d8a75fd16a03264c2750548fc`이며 기존 빌드 이후 차이는 진행 문서뿐이다.
스크립트 SHA-256은 `b838386855aa4cb81cca2c170b2bd5748d07044ae4c6b09b7af2aa133ce6074e`다.

증거: `/tmp/cat-ui-r09-attempt2-rj2eef3f/result.json`, `tool-progress.txt`,
`primary.terminal.log`, 임시 세션 transcript. 1차 결과도 원래 경로에 그대로 보존했다.
앱 코드 변경·재빌드·구성 요소 검사 반복·재시도·패키징·원격 게시를 하지 않았다.

이번 실제 PTY에서 확인한 범위:

- 로컬 작업으로 명시한 한글·줄바꿈 요청과 중간 CAT 응답이 역할별로 표시됐다.
  첫 요청이 대화 제목으로 표시됐으며 권한 창까지 정상 진입했다.
- “이번만 허용” 후 임시 작업 폴더에 실행 표식이 생겼다. 2초 대기 명령 중 서로 다른 spinner
  10개 프레임을 관찰했고, 작업이 끝나기 전에 “작업 완료”를 표시하지 않았다.
- 같은 명령은 `UI_TOOL_OK`, exit code 0으로 끝났다. 이어진 두 번째 모델 요청의
  `LOCAL_TOOL_FINISHED`와 최종 “작업 완료” 표시를 확인했다.
- 하지만 명령이 실행 중인 동안 상태 문구는 **“권한 승인 기다리는 중”**으로 남았다.
  이 문구 검사 실패로 전체 시나리오를 통과 처리하지 않는다. 앱 종료나 도구 실패가 아니다.

원인:

`src/agent/interactive.ts`가 승인 화면을 열기 전에 `approval_required`를 보낸다.
`src/tui/presentation.ts`의 `RunActivity.consume()`은 이전 도구 실행 상태를 승인 대기로 덮어쓴다.
선택 완료 시 `src/tui/screen.ts`는 modal 여부만 해제하며, 승인 결과를 받은 뒤 실행 상태를
복원하는 처리가 없어 `tool_result`가 오기 전까지 대기 문구가 남는다.
로그의 `text_complete → tool_start → approval_required → tool_result → run_end` 순서와
명령 실행 중 캡처가 이 원인을 뒷받침한다. 런타임 진행과 표시 상태가 어긋나는 UI 결함이다.

상태는 `BLOCKED_VERIFY`다. 긴 권한 창의 실제 페이지 이동·resize, 오류·취소 후 복귀,
modal/raw 포커스, `/rename`·재개·무색상 흐름은 아직 실행하지 않았다.
검증용 앱·모의 서버는 종료했고 증거는 보존했다.

다음 필요한 승인: 승인 후 실행 상태 표시 수정, `npm run build` 추가 1회,
수정 확인과 남은 PTY 흐름을 위한 로컬 검증 추가 1회. 권한 정책 변경이나 공개 배포는 포함하지 않는다.

## 2026-09-21 승인 후 상태 전환 수정

사용자가 “상태 표시 수정 + npm run build 추가 1회 + 남은 로컬 검증 1회”를 명시적으로 승인했다.
`TerminalInteractionPort.decideApproval()`에서 유효한 선택을 받은 뒤 허용은 도구 실행 상태로,
명시적 거부는 거부 결과 정리 상태로 바꾼다. 이미 취소된 signal이나 Esc 예외에는 실행 상태를
설정하지 않는다. 승인 선택값·권한 저장·실행 전 재검증은 기존 executor가 그대로 처리한다.
일반 overlay 종료를 무조건 도구 실행으로 취급하지 않으며 이벤트/저장 형식을 변경하지 않는다.

변경 범위는 화면 연결 코드 1곳이다. 로컬 검증은 이 수정과 남은 PTY 흐름을 대상으로 한다.
서로 다른 후속 응답 표식으로 이전 화면의 완료 문구를 새 요청 결과로 오인하지 않게 하고,
명시적 거부가 실제 명령 실행으로 이어지지 않는지도 확인한다. 기존 구성 요소·예산 검사 결과는
재사용하고 반복하지 않는다. 이전 1·2차 실패 기록은 보존하며 공개 배포는 하지 않는다.

소스 커밋: `d97e5faa3f62a271e747554b0330a1e6bf93cf7c`.
`npm run build` 추가 1회를 실행 전 예약했다(누적 build 2회). 기준 `origin/main`은
`94d46c6c11464ec7612abf9aeacca0790d74d212`로 변경되지 않았고 활성 Git 검사 hook은 없다.

추가 빌드 1회 PASS(종료 코드 0), 대상 `7615acf454a4ebd903842d3b15d46611e357ebdd`.
그 빌드로 남은 로컬 PTY 검증 1회를 예약했다(누적 runtime 3회). 빌드 뒤 실행 코드·입력은
수정하지 않는다. 검증 스크립트 SHA-256:
`fd0293838eb6e46392b42e4967144d2d01ab370d6af2658c9ba58e3a8626a60a`.

## 2026-09-21 3차 실행 결과 — 시작 화면 검증의 출력 순서 오류

추가 빌드 1회는 통과했다. 추가 로컬 실행 1회는 `fixture_setup`에서 종료 코드 1로 중단했다.
실행 대상: `0f86e8d3863c655f63d0a25601497b98bcce6326`(빌드 뒤 차이는 진행 문서뿐).
증거 경로: `/tmp/cat-ui-r09-attempt3-swvvscid/result.json`, `primary.terminal.log`.

검증 스크립트의 `setup()`은 `새 대화 · ask` 다음에 `( o.o )`를 순서대로 기다린다.
하지만 이번 실제 출력은 고양이를 포함한 시작 화면을 먼저 그리고 나중에 머리글·입력창만
부분 갱신했다. `expect()`가 머리글 뒤로 검색 위치를 이동시킨 탓에 이미 앞서 출력된
고양이를 찾지 못했다. 실패 로그 자체에 고양이와 머리글 모두 표시되어 있어 앱 시작 실패가
아닌 **검증 스크립트의 시간적 출력 순서 가정 오류**다. 이전 실행에서 우연히 통과했던
조건이며, 검증 스크립트 작성 시 이 비결정적 화면 갱신 순서를 놓쳤다.

이번 요청에서 완료한 것은 승인 후 상태 표시 소스 수정과 빌드 통과다. 모의 API에는 모델 목록
GET만 전달됐고 대화 POST는 없었다. 따라서 **수정한 승인 흐름은 실행 검증하지 못했다.**
앞선 예산·구성 요소 결과를 그대로 보존하되 이번 수정의 성공 증거로 대체하지 않는다.
앱 코드를 추가 변경하거나 재실행하지 않았고 검증 프로세스·모의 서버는 정리했다.
상태는 `BLOCKED_VERIFY`, 누적 build 2회/runtime 3회다. 원격 게시·패키징은 하지 않았다.

다음 필요한 승인: 검증 스크립트의 시작 화면 확인을 부분 갱신 순서에 의존하지 않도록
보완하고, 기존 빌드로 남은 로컬 검증 추가 1회. 앱 수정이나 추가 빌드는 필요하지 않다.

## 2026-09-21 4차 로컬 검증 예약

사용자 “네 그렇게 진행하세요”로 검증 스크립트 보완과 기존 빌드의 로컬 검증 추가 1회가 승인됐다.
화면의 여러 표시를 확인할 때는 같은 동작 이후 출력 구간에서 순서에 관계없이 모두 찾도록
`expect_all()`을 추가했다. 새 대화 머리글·고양이·입력 준비와 권한 창 표시를 묶어 확인한다.
동작 이전 출력은 검색하지 않고, 후속 응답의 고유 표식과 도구 실행/완료 검사는 유지한다.
검사를 빼거나 앱 동작을 바꾸어 맞추지 않는다. 입력이 없는 별도 재시작·재시도는 없다.

앱 코드·빌드 입력은 그대로이며 `7615acf454a4ebd903842d3b15d46611e357ebdd` 빌드를 재사용한다.
build 누적 2회는 유지하고 runtime 누적 4회로 실행 전 예약한다. 구성 요소·예산 검사도 반복하지
않는다. 실패하면 중단하고 증거를 보존한다. 포장·push·PR·병합·공개 배포는 이번 범위가 아니다.

## 2026-09-21 4차 실행 결과 — 상태 전환 확인, 누적 로그 판정 오류

예약한 로컬 검증 1회를 실행했다. 시작 화면 그룹은 통과했고,
`pty_text_then_tool_activity`에서 종료 코드 1로 중단했다. 실행 대상은
`fae10cd68dab872cf07f0533bbe5041d8566eb2d`, 스크립트 SHA-256은
`a4a668ebba86aa53ad89e1063c879b9096e91662e5e5ccf63e854dc847cb7159`다.
증거: `/tmp/cat-ui-r09-attempt4-cx_jg45x/result.json`, `tool-progress.txt`,
`primary.terminal.log` 및 임시 세션 기록.

이번에는 **수정한 상태 전환을 실제로 실행했다.** 승인 후 도구 명령이 실행되고,
상태 문구가 “도구 실행 중 · run_command”로 바뀐 뒤 여러 spinner 프레임에 걸쳐 유지됐다.
관찰 구간에 spinner 10종이 있었고 조기 완료 표시가 없었으며 도구와 후속 모델 요청이 완료됐다.

하지만 스크립트는 승인 키를 보내기 직전부터 수집한 **전체 출력 이력**에 “권한 승인 기다리는 중”이
단 한 번이라도 있으면 실패하도록 되어 있었다. 실제 캡처는 승인 창 종료 직후의 대기 문구 1개 뒤로
실행 문구가 연속 갱신되는 순서였다. 2차의 “실행 중에도 계속 승인 대기”와 달리 상태 전환은
이뤄졌지만 과거 프레임을 현재 상태로 취급한 assertion이 실패했다. 실패 결과를 PASS로 덮어쓰지 않는다.

명시적 거부, 긴 권한 설명의 페이지 이동·resize, 오류·취소 복귀, raw/modal 포커스,
rename·재개·무색상 시나리오는 이 실패 뒤 실행되지 않았다. 재실행하지 않았고 앱 코드·빌드도
변경하지 않았다. 검증 프로세스와 모의 서버는 종료했다. 누적 build 2회/runtime 4회,
전체 상태 `BLOCKED_VERIFY`를 유지한다.

다음 승인 요청은 검증 스크립트가 실제 현재 상태와 과거 출력을 구분하도록 보완하고 남은 로컬
검증을 진행하는 것이다. 검증 도구 자체 오류로 매번 중단되는 문제를 줄이기 위해, 사용자가 명시적으로
허용하는 경우에만 스크립트 오류 수정 후 최대 2회 추가 재시도를 요청한다. 실제 앱 오류라면 즉시
중단하며 앱 수정·재빌드·배포 권한은 요청하지 않는다. 현재는 어떤 추가 실행도 승인된 것으로 취급하지 않는다.

## 2026-09-21 5차 실행 예약 및 제한된 재시도 승인

사용자가 스크립트 보완 후 실행 1회와 **검증 스크립트 자체 오류일 때만 최대 2회 재시도**를
명시적으로 승인했다. 누적 attempt 5를 먼저 예약하고, 실패 시 원인 분류와 기록을 한 뒤에만
남은 2회 범위에서 사용할 수 있다. 실제 앱 오류에는 재시도하지 않는다.
앱 수정·재빌드·구성 요소 검사 반복·패키징·원격 게시 승인은 포함하지 않는다.

상태 검사는 spinner 행의 갱신들을 추출해 실행 상태로 전환됐는지, 이후 갱신이 실행 상태를
유지하는지, 관찰 종료 시 최신 상태도 실행 중인지 확인하도록 보완했다. 승인 결과 처리 전
이전 상태가 한 번 그려진 사실은 별도로 기록하되 실행 중 상태 고착과 혼동하지 않는다.
실행 상태가 전혀 없거나 실행 상태로 바뀐 뒤 다시 승인 대기로 돌아오면 실패 처리한다.
명령 시작 표식·여러 spinner 프레임·조기 완료 금지·도구 및 후속 응답 완료 검사는 유지한다.

## 2026-09-21 5차 실행 결과 — 승인 범위 통과

`timeout 180s python3 .git/cat-progress/P14-ui-flow.py 5`: **PASS**, 종료 코드 0.
실행 대상 `80e4778563cfa083b8cacf2fdd23f5ee4f649a09`, 기존 빌드 대상
`7615acf454a4ebd903842d3b15d46611e357ebdd`. 두 커밋의 실행 코드·빌드 입력은 동일하다.
스크립트 SHA-256: `e853ca67e248c75f97f19a50cdcefdf2f47edf4cd6b574bdf91ed1f2279ceb92`.
증거: `/tmp/cat-ui-r09-attempt5-la_lch6t/result.json`, `tool-progress.txt`,
`primary.terminal.log`, `resume.terminal.log`, `no-color-env.terminal.log` 및 임시 세션 기록.

통과한 실제 PTY 범위:

1. 초기 설정·masked key·고양이·입력 준비. 실제 계정 대신 가짜 키와 loopback 모의 API 사용.
2. 한글·줄바꿈 역할 구분과 텍스트 응답 후 도구 실행. 승인 뒤 “도구 실행 중”으로 전환되고,
   실행 상태에서 spinner 10종이 갱신됐다. 조기 완료 없이 도구와 다음 모델 응답까지 완료됐다.
3. 명시적 거부: 실행 표식이 생성되지 않았고 세션 기록은 `permission_denied`로 종료됐다.
4. 대화 이름 변경, 긴 승인 설명 페이지 이동, 20×8에서 Enter 승인 차단, 80×24 복원과
   Esc 취소, 취소 뒤 새 대화 요청에 정상 응답.
5. 모의 HTTP 400 오류 표시와 입력 복귀, 실제 요청 대기 중 Ctrl+C 취소 및 후속 요청 성공.
   저장된 `provider_error`·`cancelled` 종료 기록도 읽어 확인했다.
6. 연결 설정 취소 후 profile 미저장, 원문 보기의 색상 미혼입, Enter 복귀 후 입력·응답 성공.
7. 종료 후 `-c --no-color`로 대화 이름과 이전 기록 재개, 새 요청 성공과 새 UI 색상 미사용.
8. `NO_COLOR=1` 새 대화의 고양이·머리글 표시와 새 UI 색상 미사용. 세 PTY 앱 모두 정상 종료.

기존의 변경되지 않은 예산·구성 요소 6개 검사 결과는 1차 기록을 재사용했으며 반복하지 않았다.
전체 시간 제한 제거는 가상 시계/취소/횟수 검증 결과이고 실제 장시간 서버 운용 시험은 아니다.
실제 외부 provider API, 다른 OS·아키텍처·Node 버전, 새 설치본은 이번 검증 범위가 아니다.

이번 승인에서는 첫 실행으로 통과했으므로 스크립트 오류용 재시도 2회는 미사용이다.
누적 build 2회/runtime 5회(앞선 실패 포함)를 그대로 보존한다. 상태 `VERIFIED`, 게시 상태
`LOCAL_ONLY`다. 실행 코드·빌드 입력을 동결하고 결과 문서만 갱신했다. 새 installer 포장,
push·PR·main 병합·Release는 수행하지 않았다. 공개 배포는 별도 요청이 필요하다.

## 2026-09-21 공개 배포 요청과 버전 빌드 승인 대기

사용자가 커밋·push·공개 배포를 요청했다. 현재까지의 수정은 모두 로컬 커밋되어 있다.
GitHub 인증과 공개 저장소 권한, main 기준 SHA, merge commit 허용, workflow/보호 규칙 부재를
읽어 확인했다. 최신 Release는 v0.1.3이고 예정 v0.1.4 tag와 `cat/p14-ui-runtime` 브랜치는 없다.
설정·권한을 변경하거나 아직 push·PR·병합·Release를 실행하지 않았다.

`src/core/version.ts`, `package.json`, lockfile의 버전은 아직 0.1.3이다. 설치 bootstrap이 Release tag와
installer 파일명을 일치시키므로 v0.1.4에는 버전 정보 갱신이 필요하다. 단순히 이전 빌드를 다른 이름으로
게시하지 않고 버전 갱신 및 `npm run build` 추가 1회와 `npm run package:installer` 1회 승인을 요청했다.
기존 dist 387개 파일 hash를 `.git/cat-progress/P14-ui-dist-before-release.sha256`에 보존했다.
승인 후 버전 출력 외 생성물이 같은지 파일 hash로 대조하고, 성공한 UI 실행 검증은 반복하지 않을 계획이다.
기존 artifacts는 새 포장 전에 보존 경로로 옮기며 덮어쓰거나 삭제하지 않는다.

## 2026-09-22 v0.1.4 배포 준비

버전 정보 반영·build 추가 1회·설치본 포장 1회가 필요한 절차를 안내한 뒤 사용자가
“push하고 메인에pr해서 merge하고 공개배포 하세요”로 진행을 재지시했다. 안내한 배포 절차에 따라
버전 상수와 package/lockfile의 루트 버전만 0.1.4로 변경하고 설치·배포 문서의 버전과 검증 범위를
갱신한다. 앱 기능·의존성·installer 로직은 변경하지 않는다. UI 실행 검증은 반복하지 않는다.

origin/main은 여전히 `94d46c6c11464ec7612abf9aeacca0790d74d212`이고 원격 workflow/열린 PR/
main 보호·ruleset은 없다. merge commit은 허용된다. 예정 브랜치·v0.1.4 tag도 아직 없다.
버전 외 dist hash 동일성을 확인한 뒤 설치본을 포장하고 push·PR·main 병합·공개 Release를 진행한다.
