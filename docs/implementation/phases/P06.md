# [cat][P06] 세션·컨텍스트·rewind

## 범위

versioned session metadata와 모델 중립 transcript를 bounded JSONL로 보존하고,
new/resume/continue/fork/rename lifecycle, checkpoint 소유 rewind, 모델 context projection과
비재귀 compact를 연결한다. 원본 `src/sessions.ts`, `src/history.ts`, `src/context.ts`,
`src/checkpoints.ts`의 계약만 읽으며 원본 테스트는 실행하지 않는다. AGENTS loader는 P09,
화면·CLI 연결은 P07–P08 범위로 남긴다.

P05는 PR #5의 head `d9f9a87195fe9507f50f1f5989958cab9d3983d5`를 merge commit
`e5f082a0b8ccb5fdc70233837762941e63f7233b`로 병합했고 tree·부모·`origin/main`
정합성을 확인했다. 이 커밋이 P06의 기준 main이다.

## 하위 작업 커밋

| 작업 | commit | 변경 요약 |
|---|---|---|
| P06.1 | `6ad083a2fea6ba881b2c7c754c38cbebc0df90fc` | versioned bounded JSONL, paging, single-writer lock과 redaction |
| P06.2 | `058e6bda00e74bdd3c0abcb0abe0815a47882b0a` | session lifecycle, no-persistence와 소유권 기반 rewind |
| P06.3 | `23a4c410d5c409f38a9e181a300c2c9428aa2e94` | 도구 쌍을 보존하는 bounded 모델 context projection |
| P06.4 | `fb238f43693e6237bc94db01403d6d97de4ba821` | 기존 run 예산을 쓰는 single-pass manual/auto compact |

## P06.1 구현 메모

- JSONL reader는 64KiB chunk만 읽고 record 수, line byte, page scan byte, JSON 깊이·node와
  경고 수를 각각 제한한다. page cursor에는 파일 identity와 offset을 넣어 교체·축소를
  거부하며, 거대한 손상 line을 건너뛰는 상태도 이어 받아 다음 page가 같은 위치에 멈추지 않는다.
- 줄바꿈으로 끝난 잘못된 중간 record와 줄바꿈 없이 끝난 마지막 record를 다른 경고로 반환한다.
  뒤의 정상 record는 계속 읽되 원본 line을 삭제·수정하지 않는다.
- writer는 배타 생성한 lock file의 무작위 token과 file identity를 보유한다. PID는 진단 정보일
  뿐 stale 판정이나 자동 삭제에 쓰지 않으며, 같은 process의 동시 append와 append 중 release도
  거부한다. 소유 token이 다른 lock은 제거하지 않는다.
- 저장 디렉터리와 파일 권한은 지원 환경에서 각각 0700·0600으로 강제하고 실패를 숨기지 않는다.
  append는 손상된 마지막 line이 있으면 분리용 줄바꿈 뒤에 새 versioned record를 기록하고
  file sync를 수행하므로 기존 byte를 덮어쓰지 않는다.
- session metadata schema에는 cwd, provider/profile/model, response ID와 parent 계보만 둔다.
  trust, credential과 승인 상태는 저장하지 않으며 transcript writer 소유자만 metadata를 갱신한다.
  알려진 secret과 credential 이름의 field 값은 JSONL 직렬화 전에 제한된 복제본에서 redaction한다.

## P06.2 구현 메모

- lifecycle service는 new/resume/continue/fork/rename과 metadata update를 같은 활성 handle
  소유권 아래에서 직렬화한다. resume은 canonical cwd와 provider/profile/model을 복원하지만
  trust·credential은 복원하지 않는다. agent coordinator의 maintenance lease를 잡는 동안에는
  새 run 획득도 차단되므로 전환·rewind 도중 run이 끼어들지 않는다.
- `persistence: none`은 저장된 세션을 재개해도 새 메모리 session ID를 만들고 디스크 writer나
  임시 prompt 파일을 만들지 않는다. record 수·개별 byte·전체 byte 상한에 도달하면 오래된
  source of truth를 조용히 버리지 않고 추가 기록을 거부하며 close에서 메모리와 관리 상태를
  비운다.
- fork는 source transcript를 page와 snapshot 상한 안에서 읽어 최근 `message`와 `compaction`
  record만 새 ID로 다시 기록한다. 과거 run ID, lifecycle, agent event, permission, task와
  checkpoint 기록은 복사하지 않고 target의 격리 상태를 먼저 초기화한다.
- rewind는 현재 handle의 session ID와 checkpoint 소유자가 같은지 확인하고 활성 run이 없을
  때만 기존 안전 복원을 호출한다. 복원 결과가 완전할 때만 checkpoint manager가 record를
  제거하며, 부분 실패와 transcript 기록 실패를 별도로 반환하고 shell/network/MCP가 복원되지
  않았음을 결과에 고정한다.
- close와 metadata update는 JSONL 두 파일을 원자 transaction으로 가장하지 않는다. 각 기록과
  lock/정리 결과를 구분해 반환하고 실패를 숨기지 않으며, 영구 resume 초기화 실패는 기존
  checkpoint ownership을 지우지 않는다.

## P06.3 구현 메모

- versioned transcript message codec은 core message와 tool result를 제한된 JSON 구조로
  정규화한다. assistant tool call은 모든 대응 result가 이름과 call ID까지 일치할 때만 하나의
  history unit으로 채택하며, incomplete·orphan·mismatch는 실행 가능한 모델 문맥에서 제외하고
  제한된 경고로 남긴다.
- 모델 projection은 append-only transcript를 source of truth로 유지한 채 최근의 완전한 대화
  unit부터 message·byte 상한 안에 넣는다. 과거 tool observation은 전체·개별 byte 예산 안에서
  redaction한 purpose-specific preview로 줄이고, 호출과 결과 쌍은 함께 남거나 함께 빠진다.
- 현재 실행에서 별도로 주입한 신뢰된 system message만 system role로 보낸다. 저장 transcript와
  compaction preserved message의 system role은 승격하지 않으며, 신뢰된 system 지침이 잘리거나
  projection 상한을 넘으면 빈 bounded 결과와 명시적 requirement를 반환한다.
- context window은 provider metadata를 우선하고 없을 때만 사용자 설정을 사용한다. 둘 다 없으면
  unknown으로 유지해 128k 같은 임의 기본값을 가정하지 않는다. 알려진 상한에는 기본 85% 자동
  compact threshold를 계산하고 source·projected token 추정값을 함께 반환한다.
- lifecycle의 transcript scanner는 영구 JSONL과 no-persistence 메모리에 같은 record callback
  계약을 제공한다. maintenance lease와 handle 직렬화를 소유한 동안에만 읽고, page·record·snapshot
  byte·cursor 반복과 손상 경고를 제한하므로 run이나 append가 중간에 끼어들지 않는다.

## P06.4 구현 메모

- manual과 auto는 `ContextCompactionService.compact()` 하나를 trigger만 달리해 호출한다. auto는
  알려진 context window의 threshold에 실제로 도달한 경우에만 진행하고 unknown 또는 threshold
  미만에서는 예산을 소비하지 않는다.
- service는 `AgentRunner.run()`이나 자기 자신을 호출하지 않는다. 호출자가 넘긴 현재 run의
  `AgentExtensionBudgetPort`에서 `tryConsumeCompaction()`으로 compaction과 공통 recovery 횟수를
  함께 예약하고 `consumeModelRequest()`로 최초 모델 요청을 소비한다. 같은 budget을 provider
  retry port와 signal로 전달하며, service 자체의 재시도 loop는 없다.
- 압축 요청은 현재 provider와 model에 `tools: []`로 한 번 stream한다. context window이 알려졌으면
  고정 prompt와 출력 여유를 제외한 최근 완전한 history unit만 token·message·content-part·byte
  상한 안에 넣으며, tool call/result는 함께 포함되거나 함께 빠진다.
- 최근 사용자 의도, 계획, 권한 제한, 미완료 작업과 미완료 tool exchange는 bounded·redacted
  continuity snapshot으로 만든다. 실행 가능한 tool call로 복원하지 않고 historical assistant
  data로 보존하며, 현재 신뢰된 system 지침은 요약 대상에서 제외하고 다음 projection에서만 다시
  주입한다. 모델 summary 역시 system 지침으로 승격하지 않는다.
- summary가 비었거나 너무 크거나, 재투영 뒤 token이 줄지 않았거나 threshold 아래의 안전한
  projection이 되지 않으면 completed 경계를 쓰지 않고 `continueRun: false`의 명시적 stop을
  반환한다. 성공 시에도 원문은 그대로 두고 completed compaction record만 append한다. append가
  실패하면 일부 기록 가능성을 숨기지 않고 boundary status를 `unknown`으로 반환해 재검색을 요구한다.

## 단계 정적 검토 보완

- JSONL redaction secret은 최소 byte와 marker 안전성을 검사한다. writer 초기화 실패 때도 열린
  handle과 같은 file identity인 lock만 제거해 다른 소유자의 lock을 자동 삭제하지 않는다.
- metadata update·resume·close의 `updatedAt`은 기존 `updatedAt`보다 뒤로 가지 않는다.
  no-persistence resume도 source session의 maintenance lease 안에서 bounded history를 읽는다.
- 사용자 파일·foreground shell 도구가 `sessionStore`를 일반 경로로 읽거나 바꾸지 못하도록 기존
  sensitive path와 command policy에 세션 저장소를 포함했다.
- 모델 projection은 message/tool 식별자와 경고도 redaction하고 경고 byte를 제한한다. 미완료 tool
  exchange는 실행 가능한 call 대신 bounded call ID·이름 notice로 continuity에 전달할 수 있게 했다.
- completed compaction은 요약 자체가 본질적으로 손실형임을 `lossy: true`로 기록하고, 입력 상한에서
  추가 손실이 있었는지는 `sourceWasTruncated`로 분리한다. 이 보완은
  `e50f0a5368f6c12a01bc719cd0f96494b2a86b3e`에서 확정했다.

## 정적 코드 검토

`origin/main...e50f0a5`의 전체 diff를 한 차례 읽었다. JSONL line·page·record·JSON tree와
메모리 transcript 상한, session별 writer와 maintenance 소유권, fork 격리, rewind 완료 전
checkpoint 보존, tool call/result 원자 단위, trusted system 분리, 알려진 context window의
85% threshold, 같은 run 예산을 쓰는 비재귀 압축과 append-only 실패 경계를 계약에 대조했다.

검토 보완 뒤 `origin/main`이 기준
`e5f082a0b8ccb5fdc70233837762941e63f7233b`와 같음을 다시 확인했다. TypeScript 검사 무시,
`any` 타입 우회, 추적된 secret·원본 archive·build 산출물과 GitHub Actions workflow는 발견되지
않았다. 정적 검토만 수행했으며 세션 파일 I/O, rewind, 모델 압축, 도구와 앱 런타임을 실행해
검증했다고 주장하지 않는다.

## 단 한 번의 검증

- 허용 명령: `npm run check`
- 검사 대상 commit SHA: `13908a0e1743d632734b8673d097b3f5ab44662c`
- attemptsUsed / maximumAttempts: 1 / 1
- 실제 실행: `timeout --signal=TERM --kill-after=5s 120s npm run check`
- 시작/종료 시각: 2026-09-10T09:39:26+09:00 / 2026-09-10T09:39:28+09:00
- 명령 실행 시간 / exit code: 약 1.11초 / 0
- 결과: PASS — `tsc -p tsconfig.json --noEmit`
- 추가 검증 실행: 없음
- 전체 unit/integration/e2e, 앱·도구·TUI·외부 API 실행: 미수행

## 병합 정합성

- base main SHA: `e5f082a0b8ccb5fdc70233837762941e63f7233b`
- push된 head SHA: 미생성
- PR 번호/링크: 미생성
- merge 결과: NOT_PUBLISHED
- known limitations / blocker: 정적 검토만 수행했으며 세션·rewind·압축 런타임은 실행하지 않음

## 다음 작업

`cat/p06-sessions-context` branch를 만들고 PR 정합성을 검토해 merge commit으로 병합한다.
