# [cat][P02] 설정·인증·workspace trust

## 범위

`CAT_HOME`과 `.cat` 저장 경로, versioned 설정, API-key-only profile, redaction,
canonical workspace trust와 child environment 경계를 구현한다. 원본
`src/settings.ts`, `src/auth.ts`, `src/trust.ts`, `src/security.ts`,
`src/workspace.ts`와 관련 테스트의 계약만 읽었다.

## 하위 작업 커밋

| 작업 | commit | 변경 요약 |
|---|---|---|
| P02.1 | 현재 커밋에서 생성 | 저장 경로, 설정 precedence, bounded atomic JSON |
| P02.2 | 대기 | API-key credential와 provider profile |
| P02.3 | 대기 | trust, child env와 민감 경로 |

## 정적 코드 검토

P02 구현 완료 후 한 차례 기록한다.

## 단 한 번의 검증

- 허용 명령: `npm run check`
- 검사 대상 commit SHA: 미예약
- attemptsUsed / maximumAttempts: 0 / 1
- 시작/종료 시각 및 exit code: 미실행
- 결과: NOT_RUN
- 추가 검증 실행: 없음
- 전체 unit/integration/e2e, 외부 API, 실제 TUI, installer 실행: 미수행

## 병합 정합성

- base main SHA: `85c68d133a89ebf5578ab8a9a79618d1daba6d16`
- push된 head SHA: 미생성
- PR 번호/링크: 미생성
- merge 결과: NOT_PUBLISHED
- known limitations / blocker: P02는 구현 중이며 실제 credential을 읽거나 API를 호출하지 않음

## 다음 작업

P02.2 API-key-only credential와 provider profile을 구현한다.
